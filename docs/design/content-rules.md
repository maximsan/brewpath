# BrewPath — course content rules

**Moved.** These rules now live in
[`prototype/CLAUDE.md`](../../prototype/CLAUDE.md), which is where course
content is authored — so they load automatically at the moment they apply,
instead of being a copy that silently drifts from the one in context.

This file is a pointer so existing links keep resolving. Do not restate the
rules here; edit them at the source.
